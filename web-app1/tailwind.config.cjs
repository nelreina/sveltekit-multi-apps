const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			colors: {
				nrdark: '#011627',
				nraccent: '#87a2b9'
			}
		}
	},

	plugins: []
};

module.exports = config;
